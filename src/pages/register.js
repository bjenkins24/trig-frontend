import React from 'react';
import Layout from '../components/Layout';
import AuthBox from '../components/AuthBox';

const Register = () => {
  return (
    <Layout>
      <AuthBox heading="Create a free account" />
    </Layout>
  );
};

export default Register;
